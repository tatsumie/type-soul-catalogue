<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Accessory Cards</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f7f6;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            padding: 20px;
        }
        .top-left-container{
            position: absolute;
            top:20px; 
            left:20px; 
        }
        .add-button{
            background-color: #4CAF50;
            color:white;
            padding: 20px 30px;
            border: none; 
            border-radius: 5px;
            text-align: center;
            text-decoration: none;
            display:inline-block;
            font-size: 13px;
            margin-bottom: 20px;
            margin-left: -8px;
            cursor:pointer;
        }
        .card {
            background-color: white;
            border: 1px solid #ddd;
            border-radius: 8px;
            width: 280px;
            margin: 15px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px;
            border-bottom: 1px solid #ddd;
            font-size: 18px;
            font-weight: bold;
        }
        .card-body {
            padding: 10px;
        }
        .stat-table {
            width: 100%;
            border-collapse: collapse;
        }
        .stat-table td {
            padding: 5px;
        }
        .stat-names {
            text-align: left;
            font-weight: bold;
            padding-right: 10px;
        }
        .stat-values {
            text-align: right;
        }
    </style>
</head>
<body>

<div class="top-left-container">
    <a href="/" class="add-button">Add Accessory</a>
</div>
    <% accessories.forEach(accessory => { %>
        <div class="card">
            <div class="card-header"><%= accessory.accessory_name %></div>
            <div class="card-body">
                <table class="stat-table">
                    <tr>
                        <td class="stat-names">Health</td>
                        <td class="stat-values"><%= accessory.health %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Defense</td>
                        <td class="stat-values"><%= accessory.defense %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Posture</td>
                        <td class="stat-values"><%= accessory.posture %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Reiatsu</td>
                        <td class="stat-values"><%= accessory.reiatsu %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Meter Gain</td>
                        <td class="stat-values"><%= accessory.meter_gain %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Reiatsu Regen</td>
                        <td class="stat-values"><%= accessory.reiatsu_regen %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Reduced Meter Drain</td>
                        <td class="stat-values"><%= accessory.reduced_meter_drain %></td>
                    </tr>
                    <tr>
                        <td class="stat-names">Hierro Pen</td>
                        <td class="stat-values"><%= accessory.hierro_pen %></td>
                    </tr>
                </table>
            </div>
        </div>
    <% }); %>

</body>
</html>
